@model IEnumerable<DoAnWeb.Models.LOAISP>

@{
    ViewBag.Title = "Index";
    Layout = "~/Areas/Admin/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table table-bordered">
    <tr>
        <th>
            Tên Loại Sản Phẩm
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.TENLOAISP)
        </td>
        <td>
            @Html.ActionLink("Chỉnh Sửa", "Edit", new { id=item.MALOAISP }) |
            @Html.ActionLink("Xem Chi Tiết", "Details", new { id=item.MALOAISP }) |
            @Html.ActionLink("Xóa", "Delete", new { id=item.MALOAISP })
        </td>
    </tr>
}

</table>
